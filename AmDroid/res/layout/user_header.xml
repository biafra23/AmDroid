<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:orientation="vertical">

    <TextView android:id="@+id/name"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:layout_alignParentLeft="true"
              android:layout_centerVertical="true"
              android:layout_alignWithParentIfMissing="true"

              android:focusable="false"
              android:textSize="25dp"
              android:text="Franz Nase"
            />

    <LinearLayout android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  android:orientation="horizontal"
            >
        <!--profile pic -->
        <ImageView android:id="@+id/user_image"
                   android:layout_width="100dip"
                   android:layout_height="100dip"
                   android:src="@drawable/ic_menu_gear"
                   android:paddingRight="5dp"
                   android:paddingBottom="5dp"
                   android:paddingTop="5dp"
                   android:visibility="visible"
                />
        <LinearLayout android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_gravity="center_vertical"
                      android:orientation="vertical"
                >
            <!--follow? -->
            <TextView android:id="@+id/follow"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_alignParentLeft="true"
                      android:textSize="18dp"
                      android:focusable="false"
                      android:text="Following"
                      android:textColor="@color/light_grey"
                      android:background="@color/dark_grey"
                    />

            <!--follower -->
            <TextView android:id="@+id/followers"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_alignParentLeft="true"
                      android:textSize="18dp"
                      android:focusable="false"
                      android:text="Followers"
                      android:textColor="@color/light_grey"
                    />

            <!--following -->
            <TextView android:id="@+id/following"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_alignParentLeft="true"
                      android:textSize="18dp"
                      android:focusable="false"
                      android:text="Following"
                      android:textColor="@color/light_grey"
                    />
            <!--  Amen received -->
            <TextView android:id="@+id/amen_received"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_alignParentLeft="true"
                      android:textSize="18dp"
                      android:focusable="false"
                      android:text="Amen received: ?"
                      android:textColor="@color/light_grey"
                    />
            <!-- Original AMen -->
            <TextView android:id="@+id/original_amen"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_alignParentLeft="true"
                      android:textSize="18dp"
                      android:focusable="false"
                      android:text="Original Amen: ?"
                      android:textColor="@color/light_grey"
                    />

            <!--  Amen Score -->
            <TextView android:id="@+id/amen_score"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_alignParentLeft="true"
                      android:textSize="18dp"
                      android:focusable="false"
                      android:text="Amen score: ?"
                      android:textColor="@color/light_grey"
                    />
            <!--  Amen given -->
            <TextView android:id="@+id/amen_given"
                      android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_alignParentLeft="true"
                      android:textSize="18dp"
                      android:focusable="false"
                      android:text="Amen given: ?"
                      android:textColor="@color/light_grey"
                    />
        </LinearLayout>

    </LinearLayout>

</LinearLayout>